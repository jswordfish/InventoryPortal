function MarketFormValid() {
	
	/*$.validator.addMethod("not_allow_repeated_char", function(value, element) {
		//cannot be zero(1..)
		return this.optional(element)
				|| /^(?!.*([A-Za-z])\1{4})[A-Za-z]+$/.test(value);
	}, "Please Enter Correctly");
*/
	$("#cancelForm").validate({
						rules : {
							licenseNo : {
								required : true
							},
							
							cancelRemarks : {
								required : true
							},
							

							suspendFromDate : {
								required : true
							},

							suspendRemarks : {
								required : true
							},
							
							suspendToDate : {
								required : true
							},
							
							
							
						},
						
						messages :{
						
							licenseNo : {
								required : "Please fill License numbers "
							},
							
							
							cancelRemarks: {
								required : "Please fill this fiels"
							},
							
							suspendFromDate : {
								required : "Please fill this fiels  "
							},
							
							suspendRemarks : {
								required : "Please fill this fiels "
							},
							
							suspendToDate : {
								required : "Please fill this fiels "
							},
							
						},
					highlight : function(element, errorClass, validClass) {
							$(element).closest(".col-md-4").addClass(
									"has-error").removeClass("has-success");
						},
						unhighlight : function(element, errorClass, validClass) {
							$(element).parents(".col-md-4").addClass(
									"has-success").removeClass("has-error");
						}
					});
	
	
	
	
	if ($("#cancelForm").valid()) {
	
        return true;
        
} else {
	alert("invalid");
        return false;

}}
